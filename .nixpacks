# Configuración de Nixpacks para Trae MCP Monitor

# Provider: Node.js
provider = "node"

# Build command
build_cmd = "pnpm install --prod"

# Start command
start_cmd = "pnpm start"

# Node.js version
node_version = "18"

# Package manager
package_manager = "pnpm"